#include <stdio.h>
int main() {
    char p[100], key[] = "QWERTYUIOPASDFGHJKLZXCVBNM";
    int i;
    printf("Enter text: ");
    scanf("%s", p);
    for (i = 0; p[i]; i++)
        if (p[i] >= 'A' && p[i] <= 'Z')
            p[i] = key[p[i] - 'A'];
    printf("Cipher: %s", p);
    return 0;
}
